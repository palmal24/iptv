{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run TS with ts-node",
            "runtimeExecutable": "ts-node",
            "args": ["${workspaceFolder}/CheckLinks.ts"],
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}